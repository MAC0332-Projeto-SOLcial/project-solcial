# ============================================
# Arquivo de Exemplo de Variáveis de Ambiente
# ============================================
#
# INSTRUÇÕES DE USO:
# 1. Copie este arquivo para criar um novo arquivo chamado .env
#    Exemplo: cp .env.example .env
#
# 2. Substitua os valores de exemplo pelas suas chaves reais
#    - NÃO commite o arquivo .env no git (ele já está no .gitignore)
#    - O arquivo .env.example pode ser commitado (não contém informações sensíveis)
#
# ============================================

# Chave da API do Google Cloud
# Substitua o valor abaixo pela chave real
GCLOUD_API_KEY=chave_api_aqui

# URLs das APIs
GOOGLE_SOLAR_API_URL=https://solar.googleapis.com/v1
GOOGLE_GEOCODING_API_URL=https://maps.googleapis.com

# Server Configuration
PORT=3000
